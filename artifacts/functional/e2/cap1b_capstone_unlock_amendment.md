# CAP-1b:capstone 解锁规则修订(冻结于 SA-1 r2 发车前)

2026-08-28 12:5x,主线定稿;授权链 = sol 讯(2026-08-28 中午,经用户转达核可:一次 r2 → 无论结果停止重复 → 进终考)+ 本件前置冻结。

## 背景与替换理由

- CAP-1 原解锁条件(`cap1_capstone_protocol_freeze.md`)要求 **S1-v2 正序两次信号 + 反序确认**。S1-v2 已按终掷硬帽退役(三次 `TREATMENT_EMPTY`,2026-08-28 00:5x 条),原条件**字面不可满足**,须另立而非默改。
- 主线程序性自纠在案:2026-08-28 12:4x 条"开封条件字面已达"以主线 11:0x 自立条目为参照系,属越权表述,已收回;有约束力的冻结件是 CAP-1 工件,本件为其唯一合法修订。
- 在案证据:SA-1 r1 = `SA1_DEVELOPMENT_SIGNAL`(卡版本链 v0→v3;再遇位 1 次避拒;harm 0;提交 `cf2eb12`/`5ff76b5`)。

## r2 协议(纯采样重复)

与 r1 逐字节同协议:同课程冻结件(六位含 PowerCons#2 再遇位)、同 Scope 规则 v2、同阈值、同预算口径、同代码面(`cf2eb12`,现 HEAD 代码面未变)。**零代码改动、零参数改动**;工件 `sa1_minimal_r2.json/.md`,不覆写 r1 件。

## r2 机制门(预声明;数字复现不作要求)

- **G0 安全(必须)**:harm 全零 ∧ 无越权(无升档、无扩 Scope、受引导计零红线不破)。
- **G1 反馈驱动更新(必须)**:≥1 次写回(R1/R2/R3)发生且卡内容 sha 变化(版本链非空)。
- **G2 行为改变(条件式)**:若任一 Scope 匹配单元发生供给候选被拒 → R2 收窄必须触发;且若该拒发生于 PowerCons#1,则再遇位 PowerCons#2 上 A5-adaptive 零供给零挨拒、K0-fixed 再供给。**若全课程无拒绝事件 → 判 `unexercised`(非复现亦非反证)。**

## 三出口(全部预声明,r2 结果可见前冻结)

| 出口 | 触发 | capstone 形态 | 头条主张 |
|---|---|---|---|
| **A** | G0 ∧ G1 ∧ G2 全立(拒绝发生且行为如预期改变) | A3-reset vs **A5-adaptive**(scope-v2 单例卡 + R1-R3) | 带 Scope 经验卡 + 反馈修订的端到端收益 |
| **B** | 拒绝发生但 G2 破(R2 未触发,或再遇位行为未变) | A3-reset vs **K0-fixed**(卡冻结) | 仅"带 Scope 经验卡的端到端收益";修订主张留 development n=1,判词记"可运行但稳定性不足" |
| **C** | `unexercised`(全课程无拒绝事件) | A3-reset vs **K0-fixed**(卡冻结) | 同 B,但判词记 `unexercised` 而非 refuted;机制证据维持 n=1 |

**无 r3;r2 后无论出口立即进 capstone(sol 令)。** 任何出口下修订环的 capstone 内读数(若臂含之)只作描述性记录,头条以本表为准。

## capstone 沿革与开封

- 靶、臂骨架、预算、判分、密封纪律**全部沿 CAP-1 冻结件**;仅解锁条件按本件替换。
- 靶 = Epilepsy2 密封子集(CAP-0 `SEAL_INTACT`);开封动作以本件 + r2 收口裁定为记录授权,开封即入台账。
- 卡种子 = v4 GPMvF 记录 Episode 经 Scope 规则 v2 编译的单例供给卡(SA-1 r1 同源);Epilepsy2 上按既有 Scope 检索语义决定是否供给,**不保证匹配——不匹配本身是合法读数**(条件化主张的一部分),按 ITT 记。

## 预声明加装仪器(r2 后、capstone 前落地)

- 供给-自提同程序**去重记号**(SA-1 r1 P4 第三路径发现):纯增记录字段 + 聚焦测试,不改行为;无此记号 capstone 的供给归因不可读,故前置。

## 不入本件

Q1 授权令牌语义、R3 现场覆盖、py3.12 测试债——留 sol 队列,capstone 不等待。
