# 数据就绪 Domain Workflow：后续交付计划

日期：2026-09-17。Planner：Astra。执行负责人：Opus。
状态：交用户转发的计划；本次仅完成文档与 T-only 资格检查，未启动拟合或实验 API。

**当前安排（2026-09-18更新）**：短适配包已完成；它是小MLP代理，不能据其结果否定LTSV/TimeInf或TSFM。当前转发任务为[LTSV核心机制与修复信号试验](DEV_DATA_READINESS_LTSV_CORE_REPAIR_SIGNAL_TASK_2026-09-18.md)：真实预训练Time-MoE、两个固定开发批次、逐块独立更新与同TSFM适配确认，详见第8节。继续直接修复数据，不加入降权/筛除；不启动新Skill或画像路由课程。第1—7节保留为历史安排及实际回执。

## 1. 交付目标

完成以下可运行、可复跑的系统：

域内开发过程与反馈 → Slow 形成 Workflow guidance → 开发集实测选优
→ 冻结域 Skill 与参考画像 → 新批次按画像选择 Skill
→ Fast 观察、构造、评估与提交 → 输出冻结后外部评分。

Workflow 包含观察、假设、实验组织、材料构造、选择和停止；算子组合只是 Fast 的产物之一。
不要求无卡 Fast 先显著超过简单方法，才允许学习 Skill。固定处理与 Random 仍保留为对照。

本轮 Test 指“冻结 Skill 在新批次上的测试”，Fast 仍可使用本批 C_A。
最终 E 不回流。这不是完全零反馈部署的 Natural Final，后者继续封存。

## 2. 当前状态

- 数据就绪 Source → 形成 → Select → Target 已真实运行。
- RD02 的 R2 修订在两个后续批次与旧卡交付完全相同，测得修订收益为零，成本更高。
- 有卡相对无卡只有一处 +0.023、配对 SE 0.042 的小规模线索，尚无稳定效用证据。
- RD01 原人口后期缺失阻断，旧选优为 NO_SKILL。
- 画像仍含增强路径的全有限数据、32 人口、scope 字段假设，需要适配。
- 恢复未评估方案的缺陷要修复；已开标签的旧失败分支不补跑。

当前欠“画像接线 + 第二域有效设置 + 同场比较”，不追加 RD02 第三轮文案修订。

## 3. 现在分发的一包

执行规格：[DEV-DATA-READINESS-PROFILE-TRANSFER](DEV_DATA_READINESS_PROFILE_TRANSFER_TASK_2026-09-17.md)。

由同一负责人连续完成：

1. 最小恢复修复；别人已完成则复用，不重复修改。
2. 适配缺失感知、不同合法人口的画像；Router 不接收人口数量、源名或效果。
3. 在 KDD 新固定人口上生成两条 Source 轨迹、一次 Slow 提案，两个开发批次选卡。
4. 保留 RD02 在 R2 开发阶段已选中的 C1；不根据测试 E 改回旧卡。
5. 冻结后，每域两个预定批次比较无卡、Generic、已知域加载、画像加载、Random与固定基线。
6. 交付一个主报告、方法说明和可复算结果表，分别验收行为、效用与成本。

预算：正常拟合最多204次，加全包2次同配置故障重试；最多6M已知token、
374个逻辑请求，实验墙钟8小时。形成、选优、Router、Fast均计入。
按前两包速度估计，真实运行约2–4小时，非保证；实现适配另需时间。
执行到上限即收口，不因无正号继续加实验。

## 4. 之后如何收口

| 第一包结果 | 后续动作 |
|---|---|
| 已知域 Skill 有收益，画像加载保留收益 | 整理完整方法图、主表、消融和复跑入口；独立终验另冻协议 |
| 已知域 Skill 有收益，画像加载丢失收益 | 只调查实际路由/scope错误，保留Skill |
| 两种加载都未显示收益 | 交付完整原型与可信负结果；不默认再修卡，继续优化须先定位具体失败 |
| KDD 仍选择 NO_SKILL | 保留此结果；可测试单卡适用/弃权，不声称两张有效Skill匹配 |
| 部分不可评分或技术未完成 | 报有效配对和缺失，不换批次凑正表 |

不临时加seed、不改评价指标/标签权限、不自动开放Natural Final。
“开发选中”与“证明长期有效”分开，不预先保证研究出现正结果。

## 5. 项目交付清单

- 一个入口：数据/Consumer/预算 → 画像 → 冻结guidance → Fast → 提交材料。
- 方法说明：开发者Opus与系统内Slow、Router、Fast分开。
- 两层切分图：Skill开发/测试；每Job内T、C_A、C_B、E。
- 主表：Known/Matched Skill对无卡、Generic、Random和固定基线。
- 机制表：加载、scope、观察/构造/实验/提交变化。
- 成本表：一次性学习、Router、Fast、拟合；复用与独立部署成本分开。
- 零增益、不确定、不可评分、技术失败均如实保留。

## 6. 当前推进更新：训练长度修正，继续直接修复数据（2026-09-17）

用户最新确认：
- 先把过拟合诊断落实为100/250步与原2000步的实际比较；
  开发选择并冻结后，到预定后续批次检查是否缓解。
- 项目继续直接修改、修复数据，不采用downweight/drop、采样或训练混合权重。
- 核心目标仍是改进域内Workflow及其后续效用，不要求无卡Fast先显著胜出才能学习Skill。

PROFILE-TRANSFER已完成：第二域未选出卡；当前单卡画像加载没有效用证据。
新的训练诊断提示2000步可能过度训练，但只是已曝光批次上的诊断；
尚未把短训练接入主实验，也不能据旧E直接宣称改进已通过独立检验。

现在分发：
[DEV-DATA-READINESS-TRAINING-LENGTH](DEV_DATA_READINESS_TRAINING_LENGTH_TASK_2026-09-17.md)。
四个固定开发Job校准公共训练长度，两个固定后续切点检查；三份固定直接修复材料。
仅改训练步数，不同时改正则、模型、评价窗口或数据权限。
最多88次拟合尝试，0实验LLM/API，0新SHA；详情以任务书为准。
状态为任务书已完成、待交Opus执行；不是实验已启动。

后续顺序：
1. 完成本包，区分预测质量、处理差值精度、时间冲突与成本。
2. 若支持短训练，下一包各臂共同使用冻结配置，重新收集域内经验、
   形成不同Workflow并在后续同域批次比较有卡/无卡/通用/简单搜索。
3. 若不支持，先针对剩余具体训练问题决定一次小修正；不反复扩大seed或修卡追正号。
4. 画像路由暂缓；直接修复空间和Skill主线保留。

本节是当前安排。第1—5节保留为PROFILE-TRANSFER派工时的历史计划，
不将其“下一包”描述误读为再次启动同一实验。


## 8. 执行回执：LTSV 核心机制与修复信号试验（2026-09-18，Opus）

任务书 [DEV-DATA-READINESS-LTSV-CORE-REPAIR-SIGNAL](DEV_DATA_READINESS_LTSV_CORE_REPAIR_SIGNAL_TASK_2026-09-18.md)（§11 收口），报告 `_scratch/dev_data_readiness_ltsv_core_repair_signal/REPORT.md`。

已 COMPLETE：真实 Time-MoE-50M、413 次逐块一步更新 + 18 条 100 步真实适配（共 2225 次更新，上限 2440），0 失败，数值运行 52 分钟，0 实验 LLM/API。
- 找到了作者首个提交里的估值脚本（未执行），其损失与优化器与论文文字不同，差异已逐项记入 METHOD.md；预测函数因与官方 generate 对不齐做过一次实现修订，修订后逐位相同。
- 估值差值只有参考损失的千分之几，8 个参照格里只有 1 个可分辨且与估值同号；只有 RD01B_Q1 三条规则选出不同材料，那里 R_LTSV 选中的材料在两个 Consumer 的 E 上都最优，但区间跨 0；MLP 迁移全部 TRANSFER_UNRESOLVED。
- 成本上估值约为逐方案确认的 1/2.5，不是数量级优势。
- 执行方单项建议：先改参考时间设计，再谈 TSFM 估值；不晋升 Skill、不扩 seed。下一项待 Planner。

## 7. 当前推进：廉价短适配反馈可行性（2026-09-18）

训练长度包完成86条轨迹。100步在RD01B_STP1改善约18%，在RD02_STP1恶化约91%；配对SE下降未解决时间外推。故不直接采用100步，也不将一次失败外推为所有逐作业早停无效。

用户希望评价器能够减少逐候选完整训练，为后续TSFM留出计算空间。本轮任务：
[DEV-DATA-READINESS-SHORT-ADAPT-SIGNAL](DEV_DATA_READINESS_SHORT_ADAPT_SIGNAL_TASK_2026-09-18.md)。

- 四个已曝光开发Job、三份固定修复材料，统一Linear服务输入，只估训练材料变化。
- 12个100步公共测量起点，36条25步适配；step1附报，0新增2000步完整训练。
- 复用原完整模型，以独立训练seed对照同块近似和后续选择；2000步是当前Consumer条件参照，100步敏感性单列。
- 计划48条短训练/适配，含检查与重试最多52条；0实验LLM/API、0新SHA。
- 这是LTSV启发的方案级小模型试验，尚非原论文复现、TSFM性能或Skill效用证据。

状态：任务书可转发给Opus，尚未启动。完成后依据近似能力、后续选择代价和真实成本决定下一项；不自动加模型、调参数或扩大样本。

**执行回执（2026-09-18，Opus）：** 已 COMPLETE，50/52 条短轨迹、0 失败、0 LLM/API、0 新完整训练，数值运行 84.6 秒；详见任务书 §11 与 `_scratch/dev_data_readiness_short_adapt_signal/REPORT.md`。REF2000 同块八格全部不可分辨，本包不能验证排序；REF100 可分辨 3 格中 25 步代理只对上 1 格。R25 在 4/4 Job 复现 R2000 选择（3 个 Job 靠小于一个 SE 的差距），该选法 E 上比始终 Linear 差 2.03%（不可分辨）。每 Job 墙钟约 14 秒对估计 102 秒，主要节省来自进程数。执行方单项建议：先改参考时间设计，不转 TSFM 小试；当前代理配置不晋级。下一项待 Planner 决定。域内Workflow/Skill学习仍是项目目标，画像路由暂缓。

## 8. 当前推进：LTSV核心机制与直接修复信号（2026-09-18）

Planner复核：上一包按任务书运行了小MLP的100步起点＋25步适配；未使用预训练TSFM、逐块独立估值或原文的分块汇总。其主参照不可分辨，不能将该结果扩写为LTSV无效或TSFM不值得试。第7节执行回执及历史数值保留，执行方建议不视为本轮决策。

现在分发：
[DEV-DATA-READINESS-LTSV-CORE-REPAIR-SIGNAL](DEV_DATA_READINESS_LTSV_CORE_REPAIR_SIGNAL_TASK_2026-09-18.md)。

- 单一真实预训练Time-MoE；作者估值脚本若仍不可取得，按论文公式重实现并明确来源，不能冒充完整论文复现。
- RD01B_Q1、RD02_T1两个已曝光开发Job；三份原修复材料、固定原始y和Linear服务输入。
- 最多176个代表父窗、528个独立单步更新；保留块级与覆盖汇总，配对比较原材料和修复材料。
- 18条同TSFM的100步真实适配，单独确认完整材料排序与后续收益；24份原MLP缓存仅作迁移读数。
- 正常2,328次优化器更新，含检查与最多一次故障重试硬上限2,440；0实验LLM/API、0新MLP训练、0新SHA。
- 资源预检后运行，数值预算3小时，实施至报告6小时；资源不足不能静默换回小MLP。
- 项目适配（192→48、固定C_A context、直接修复验收）和论文五折/筛选验收的区别开跑前列明；这是核心机制试验，不是完整benchmark复现。

状态：任务书已定稿、待用户转发，尚未运行。该任务只检查候选反馈，不改正式Consumer，不晋升Skill，不重开画像路由。
后续按“实现正确性／同TSFM修复效用／MLP迁移／实际成本”分别裁定，不再把这四个问题混成一项成败。


## 9. 执行回执：零 LLM 增强算子开发探针（2026-09-18，Opus）

依据：用户在对话中直接决定（原文见 `_scratch/dev_data_aug_operator_probe/METHOD.md`）。探针有界、0 次 LLM，保留固定默认、随机选择和短训练对照；主要读数是实际交付收益、选择遗憾和成本；E 上的事后最优只作开发线索；不设"必须全过才能接回 Harness"的门槛。

- **执行**：COMPLETE。4 个已曝光 Job × 24 份材料（None、3 个旧算子、20 个按 TempoPFN 描述自行实现的单算子）× 3 个 seed，共 288 次训练，外加 1 次检查训练，0 次失败。None 与 TRAINING-LENGTH 缓存逐位一致，增强重跑也逐位一致。修订 1：外部 CPU 竞争，墙钟上限从 2.5 小时改为 4 小时，科学配置不变。
- **效应量**：增强的效应比修复大一个量级。RD01B 上 20/23 和 22/23 个候选让 E 变好，中位数 +6% / +9%，seed 标准误约 4%。RD02 上 C_A 和 C_B 显示有益，E 却变差；RD02 的 E 由单个极端起点主导，占 68–83%。
- **交付**（RD01B / RD02 域均值，E）：
  - FixedMixup：+10.8% / −31.1%
  - R_CA：+13.2% / −26.4%
  - 短训练 None@100：+23.6% / −60.8%
  - 联合选择（材料和训练长度一起按 C_A 选）：+28.0% / −26.7%
  - 冻结的 4 Job 汇总被 RD02_STP1 主导：R_CA −6.6%，Fixed −10.2%，联合选择 +0.7%。
- **选择价值**：R_CA 比 Fixed 多 +0.1 / 0 / +4.8 / +9.5 个百分点；预算 2 份的版本 ≈ Fixed；新算子库比旧库多 +0.1 / 0 / −1.1 / +9.5 个百分点。C_A 与 E 同向率：RD01B 0.74，RD02 0.43–0.57。
- **成本**（每 Job，无竞争估计）：Fixed 38 秒，预算 2 份 147 秒，全枚举 910 秒，短训练 14 秒。
- **单项建议（未执行）**：先用 0 训练读已有预测，诊断 RD02 的 E 在极端起点上是不是被增强或短训练平滑掉了，再决定增强是否接回 Harness。报告：`_scratch/dev_data_aug_operator_probe/REPORT.md`。

**勘误（2026-09-18，DEV-TEMPO-AUG-SOURCE-ALIGNMENT §3.1；原数值文件未改，详见旧 REPORT 文末勘误节）：**
- **收益单位**："R_CA 比 Fixed 多 x 个百分点"的单位是该 Job 3-seed 平均 E_None@2000 的百分点，不是相对 Fixed 的提升。
- **配对 SE**：R_CA − Fixed 为 RD01B_Q1 +0.1 / 4.5，RD01B_STP1 +4.8 / 4.1，RD02_T1 0 / 0（同材料），RD02_STP1 +9.5 / 24.2（逐 seed −1.0、−26.2、+55.6）。四个都未分辨，撤回"按 C_A 稳定拿到搜索增量"。
- **效应量**：撤回"两域都远超噪声"。RD02_STP1 上候选效应绝对值中位数 8.4%，小于配对 SE 中位数 12.1%。
- **"方向分域 / RD02 不适合增强"撤回**，改为"这四个批次上存在明显的材料与时间条件差异"。
  - MixCross40、FreqMask10 在四个 Job 的 E 均值都为正，只作事后开发线索。
  - MixCross40 在 RD02_STP1 +8.8，SE 2.43，n=3 的 95% t 区间 [−1.7, +19.3]，不是 95% 确认。
- **口径**：
  - R_CA 是本池内 C_A 均值 argmin，不是利用 C_A 的 Agent 的上界。
  - R_Random 是单次均匀抽取的期望，不是同预算随机搜索。
  - 同批材料共享时间环境，不能当作独立案例。
- **单项建议已由后续任务书处理**：峰值诊断按新任务书 §3.2 完成，不再作前置门。
  - 两个 RD02 高损失 origin 都是全批高值时段，所有模型都未预测出。
  - 增强材料没有比 None 更多地削峰，伤害分散在多个 origin。
