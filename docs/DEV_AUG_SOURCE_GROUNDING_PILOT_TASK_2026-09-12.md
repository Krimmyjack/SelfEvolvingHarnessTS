# Source建议与证据对应：单机制开发任务书

> 执行回执：已由Opus以PARTIAL_METHODS收口；A5_current不完整，主比较UNKNOWN。详见现行计划§6.28与原执行报告。以下保留派发时规格，不表示仍待启动。

日期：2026-09-12。原派发状态：根完成方案，供用户分发。主责建议Opus；Grok仅在交付后复核关键原始读数，不设置串行审批链。项目AGENTS.md优先；一层委派，0新增哈希，保留H1/H2/H3、诊断和已有数据包。

## 1. 这一包要回答什么

**在相同Source材料、相同Target流程下，把Source中的经验结论与未经检验的推测区分开，是否能够改善后续候选探索与交付？**

本包改动面只有Source形成指令。不同时修改General、Target Slow、Specific匹配、校准块选择、评分、Consumer或增强动作。多时间块复核暂不加入。本包检验Source内容的使用方式，不宣称是新的完整self-harness算法。

既有H3已证明卡可加载；不再安排一轮通用接线审计。根已查明三类Source语句：

| 原Source内容 | 现有证据能支持到哪里 |
| --- | --- |
| 单步TimeMixup值得先试 | 四个历史作业固定增强结果支持将其作为有依据的候选先验；不保证新作业或每实体都赢 |
| 极端实体以identity为唯一建议回退 | 尚无按该可见条件组织的共同干预对照；可作推测，不能冒充已学得的条件效果 |
| 统一配方优先，差异化仅限明确极端实体；两步不作首选 | 历史试验主要是统一单动作，不能由未测过异质计划/两步推出它们有害；有限预算下先试有支持方案可以是合理启发式 |

后两类不是已证明有害，也不要求模型反向优先探索它们。本包检验：把证据不足的使用边界明确为可检验建议，能否减少不必要的探索限制。不能手工写入具体赢家、极值阈值、正确Skill或数据ID答案。

## 2. 两个Source版本

- `A5_current`：精确使用H3已形成的Source三元组及正文，来源为`_scratch/ts_aug_harness_h3_pilot/state.json`的`source|r0`。不重写旧卡，不将H3 Target/E放入其形成证据。
- `A5_evidence`：同一份a30/a40/a50/a60 Source摘要、同一模型、原SOURCE_SCHEMA、原长度上限，只增加一段形成要求：陈述建议时区分已观察效果和未经检验的推测；范围/比较对象与真实试验一致；未试过不等于负效果；不要凭汇总均值编造实体级阈值；无法给出窄条件时允许显式宽Scope或ABSTAIN。可以保留有依据的优先级，也可以提出明确标为待检验的启发式。

两者Target端完全共用H3路径。新Source只形成一次；暂态错误最多按H3规则重传，不因答案保守、宽Scope、ABSTAIN或没有变化重抽。不得为让新卡看起来更好，手工删除identity、强制多样性或强制非identity。

Source形成使用原摘要构建器及已曝光历史，不扩大为完整原始Episode进入Fast。若H3保存了实际形成输入，优先精确复用；否则使用同版本构建器，记录输入来源，不声称逐字相同已核实。实验模型须与H3约定的请求/返回标识一致；若不可用，报告不满足条件，不静默换模型。

将Source正文差异及每项建议引用的已存在证据放入本包报告即可，不建新Skill平台、证据数据库或多级schema。新Scope仍走原三值判断；MATCH不是科学效用判定。新旧Source形成成本分别记账，旧版复用的形成成本不是零。

如果新调用失败或ABSTAIN，正常收口为未形成可比较的新Source，不以空卡兜底声称完成A5比较。如果仅有措辞改写、没有可指出的建议或使用边界变化，记录这一事实，不自动进入昂贵Target课程，也不强行再抽一张。

## 3. 隔离实现与必要检查

在新的`_scratch/ts_aug_source_grounding_pilot/`下做薄适配，不改原H3或canonical代码。两种A5的差别仅为传入同一`run_agent_arm`逻辑的初始Specific；为状态/缓存增加清楚的版本命名空间，沿用既有缓存语义，不新增哈希。不要为了支持新名字复制两套不同算法。

一个必要smoke覆盖：Source确实不同且传到对应Fast；两种A5同随机初始化、同父对采样、同动作对应donor；Target更新/选择权限一致；方法失败独立标记UNKNOWN，不能让Fixed兜底成为A5主成绩。无需重新跑143项旧仪器测试或另建测试平台。

**尤其不要把“新Source提示更严谨”直接计作方法进步。** 必须在真实Target中观察它怎样影响候选和交付。

## 4. 唯一Target与数据边界

建议开发单元：Electricity `a94/r0`，采用同一TSLib CSV和已冻结32实体roster。沿用H3的MLP、训练设置、L=192、H=48、672小时训练、全部合法父窗、归一化与权重；沿用同一动作语义。根据既有T=26304：

- 截止点`t = 24*floor(.94*26304/24) = 24720`。
- T区间`[24048,24720)`，与H3训练材料不同，恰在H3最后E真值结束之后。
- C_A origins：24720、24768。
- C_B origins：24816、24864。
- E origins：24912、24960、25008、25056；末端25104。

启动前只依据路径、切分记录和曝光台账确认资格/并行任务占用，不读E数值作筛选。如果a94已被其他方法开发使用或被占用，本包不自行挑下一段，返回具体冲突；这是数据前提不成立，不是模型负结果。整文件仍是development；即使当前方法尚未看过该E，也不包装成Natural Final或新数据源。

现行seed映射增加明确的新job条目即可，保留r0模型seed。运行前将job/augmentation/batch/random-search实际整数seed记录到配置，各臂相同用途共用同值；不按结果改seed。数据权限仍是训练与C_A适应、C_B选择、所有输出冻结后E统一评分；新Source不读取本Target反馈。

## 5. 七个方法，22次计划拟合

| 方法 | 候选预算 | 作用 |
| --- | ---: | --- |
| Static | 1 | 无增强参照 |
| Fixed-Mixup | 1 | 已验证的固定增强 |
| Random | 4 | 同H3的0实验LLM搜索 |
| F | 4 | 固定H、当前任务研究 |
| A3 | 4 | 空H0起步、Target更新 |
| A5_current | 4 | 既有Source、Target更新 |
| A5_evidence | 4 | 仅Source形成方式改变、相同Target更新 |

沿用H3的q1/q2、一次更新、q3/q4和C_B选择规则；同样记录每方法实际完整性。所有候选共享训练几何不变，绝不按实体跨模型拼分。q4新H效果与最终交付分开，影子E最优不能回改选择。

上限：22次计划共享拟合，24次物理拟合硬上限（最多2次必要失败重试）；1000次API尝试、400万已计token、150分钟活跃墙钟硬上限。每个LLM方法最多200次尝试、120万已计token；全局上限优先，失败请求未知token单列。Source一次形成计入总预算，逻辑反馈/精确缓存/物理拟合分别记。预算是计划而非实测耗时承诺。

可复用H3按方法隔离及已有并发方式；每个方法内部依赖顺序保持。持续传输故障令相关方法INCOMPLETE，其余方法继续；不把技术失败、ABSTAIN和identity结果混用。输出只需一个逻辑Runner、result.json、REPORT.md、必要原始响应/配方/模型/冻结预测，沿用已有字段，不新增manifest平台。

## 6. 预先写清读数怎样解释

主机制比较是`A5_evidence vs A5_current`的交付E NMSE；同时报告它相对A3、F、Random、Fixed的结果，原始MAE和实体/origin分解只作辅助，不更换主判据。单作业单seed是开发pilot，不报统计显著或跨域成功。

报告需要直接回答：

1. 新Source改变了哪条建议的适用范围或强度？证据依据是什么？
2. 这项变化是否在实际配方、q2实验、Target更新或交付中体现？仅加载/措辞变化不能回答此问。
3. 哪个新旧完整联合候选表现不同？不能将单实体损失差当成它自身算子的因果标签。
4. 在相同Target预算下是否有真实交付改善？是否仅恢复Fixed，是否仍被F/Random解释？
5. 与旧Source相比的改变，有多少是形成模型单次随机性的可能影响？保留这个限制，后续确认才做多条独立Source形成/执行轨迹。

分支：

- Source有文案变化但实际决定相同：没有观察到有效机制，不追加格式改写。
- 候选变了但交付无改善：本设置尚未支持该改法，不改指标/窗口继续追正号。
- 胜旧A5但不胜强固定/无Source搜索：只支持Source使用方式改善，不支持完整Harness积累优势。
- 完整交付相对旧A5及强对照均有正向线索：才安排新的训练作业和独立Source形成重复；本包不自动扩第二Target。
- 有用候选仍因C_B排序未延续而未交付：保留候选线索，不改C_B；反馈代表性仍是未解决的问题，本包没有声称解决它。

## 7. 后续self-harness放在什么位置

本包不是“多作业共性错误修复”的完成版。若Source使用方式形成有用线索，下一阶段才把自然发生的完整研究轨迹带入Slow的合法经验整合：哪些Scope匹配、实际执行、实验决定及反馈相互对应，哪些相同first-fault在不同作业重现，再只开放一个得到证据支持的General或过程面修订。不能将多实体负分、多个identity结果或旧传输故障拼成共同研究错误。

如果本包仍不能提供超过好程序先验的信息，优先承认当前Source材料不足或该使用机制无增量；不直接扩大到“全部Harness无效”。这是一项开发机制检验，不把提示词改写、测试通过或一个正号当成VLDB贡献。
