Draw a clean academic framework figure for a database / ML systems paper (VLDB style) titled "Learning Consumer-Aware Data Preparation Skills for Time-Series Forecasting with an Agentic Harness".

STYLE
- Flat vector illustration, white background, landscape, aspect ratio about 2:1 (e.g. 2400 x 1240 px).
- Sans-serif font (Helvetica / Arial / IBM Plex Sans look). Dark navy text (#1A2230). Light grey panel backgrounds (#ECEFF3), white boxes with thin grey borders (#C4CBD6), rounded corners.
- Exactly one accent colour, teal (#1F6F8B), used only for the learned-knowledge path. One red (#B4412F) used only for the information wall and the blocked arrow.
- No 3D, no gradients, no shadows, no robots, no people, no emojis, no decorative clip art. Tiny line-chart glyphs are allowed inside the two data boxes.
- Render every label EXACTLY as written below, spelled correctly, and add no other text, numbers or logos.

LAYOUT: three panels from left to right, each with a bold title at its top-left and a small grey subtitle.

Panel (a) title: "(a) Offline learning"   subtitle: "Source and Select cases · Consumer scores available"
- Top box: "Source cases" (subtitle "train and score freely") with a small line-chart glyph.
- Two arrows go down from it to two boxes side by side: left box "No-card agent runs" (subtitle "inspections · choice · reason"), arrow label "run agent"; right box "Scripted records" (subtitle "Consumer score per program"), arrow label "score every candidate".
- Both boxes feed, with arrows labelled "census", into a teal-outlined box "Slow agent" (subtitle "consolidates experience").
- Teal arrows go from "Slow agent" to two small teal document/card icons labelled "candidate skill cards".
- Teal arrows go from the two cards into a box "Select on separate cases" (subtitle "score of each card's committed plans").
- A teal arrow labelled "freeze" goes right into panel (b).

Panel (b) title: "(b) Zero-feedback deployment"   subtitle: "new case · no downstream score"
- Top-left box: "New case" (subtitle "deployment-visible data") with a small line-chart glyph; arrow labelled "observes" into the central box.
- Central box with a thicker border: "Fast agent", containing four small tool chips in monospace: "inspect data", "check material", "history profile", "commit".
- Bottom-left: a filled teal card icon "frozen card" (receiving the "freeze" arrow from panel a); a teal arrow labelled "read-only guidance" goes up into "Fast agent".
- An arrow goes down from "Fast agent" to a box "Committed plan" (subtitle "frozen before any fit").

INFORMATION WALL: a vertical red dashed line between panel (b) and panel (c), full height, with the label "information wall" above it and a small grey line "no downstream score crosses leftward".

Panel (c) title: "(c) Consumer-aware preparation"   subtitle: "one harness, different Consumers"
- The arrow from "Committed plan" crosses the wall and splits into three rows.
- Row 1: small chip "augmentation program" -> box "Trainable forecaster" (lines: "MLP · DLinear", "trained on prepared data").
- Row 2: small chip "context policy / length · scaling" -> box "Pretrained TSFM" (lines: "Time-MoE, zero-shot", "reads prepared context").
- Row 3, drawn with DASHED outlines and a small grey note "cross-task extension": chip "augmentation program" -> box "Classifier" (lines: "FCN · UCR datasets", "trained on prepared data").
- At the bottom a red dashed arrow labelled "test outcome · scored once, after every plan is frozen" runs from panel (c) leftward and stops at the red wall with a red "X"; next to the X, small red text "never returned to the agent".

Keep generous whitespace, align boxes on a clean grid, keep all arrows orthogonal (horizontal and vertical segments only), and make every label readable at print size.
